; ===========================================================================
; PCM soft mixer core — voice-outer, i8sat, R = 175, unroll 3, paced feed
;
; GENERATED by tools/gen-mixer.mjs — do not edit. Regenerate instead.
; Design and measured cost: docs/driver.md §5.3 / §5.3.1.
;
; Included by src/engine.z80 (and by the bench image). The includer owns the RAM
; map and the PV_* struct offsets; this file defines only PCM_MIX_R and the code.
;
; Each voice owns the register file for its whole pass and writes into a
; frame-long mix buffer. The inner loops carry NO loop/end bounds check — the
; caller splits the pass into segments that cannot cross a boundary, which is
; exactly what a voice-outer pass buys.
;
; The DAC feed is INTERLEAVED, not a pass of its own (§5.1): the head of every
; unrolled iteration writes one sample of the plane the LAST frame finished, and
; the pad after it holds the iteration to 340 cycles. Three passes of R ticks
; carry R samples, so the frame's feed comes out at its own rate by construction.
; ===========================================================================

PCM_MIX_R   equ 175
MIX_UNROLL  equ 3
PCM_MUTE_SH equ 8      ; PV_SHIFT meaning "silent, keep advancing"
PCM_IDLE_SH equ 9      ; PV_SHIFT meaning "no voice here at all"
PCM_PASSES  equ 3      ; passes per frame — the feed's cadence
PCM_TICK_CY equ 340     ; the period one paced iteration holds to
PCM_SEG_MAX equ 31      ; debt_tab's last entry (segments per frame)

R_FIRST_BEG:
mix_first_s0:
mix_first_s0_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(G_PAD)         ; pace: hold the tick to the frame's period
pd1:
        dec  a
        jr   nz,pd1
        ld   a,(de)             ; sample
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np2
        inc  d
np2:
        ld   a,(de)             ; sample
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np3
        inc  d
np3:
        ld   a,(de)             ; sample
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np4
        inc  d
np4:
        dec  b
        jp   nz,mix_first_s0_lp
        ret
mix_first_s1:
mix_first_s1_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(G_PAD)         ; pace: hold the tick to the frame's period
pd6:
        dec  a
        jr   nz,pd6
        ld   a,(de)             ; sample
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np7
        inc  d
np7:
        ld   a,(de)             ; sample
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np8
        inc  d
np8:
        ld   a,(de)             ; sample
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np9
        inc  d
np9:
        dec  b
        jp   nz,mix_first_s1_lp
        ret
mix_first_s2:
mix_first_s2_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(G_PAD)         ; pace: hold the tick to the frame's period
pd11:
        dec  a
        jr   nz,pd11
        ld   a,(de)             ; sample
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np12
        inc  d
np12:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np13
        inc  d
np13:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np14
        inc  d
np14:
        dec  b
        jp   nz,mix_first_s2_lp
        ret
mix_first_s3:
mix_first_s3_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np16
        inc  d
np16:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np17
        inc  d
np17:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np18
        inc  d
np18:
        dec  b
        jp   nz,mix_first_s3_lp
        ret
mix_first_s4:
mix_first_s4_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np20
        inc  d
np20:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np21
        inc  d
np21:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np22
        inc  d
np22:
        dec  b
        jp   nz,mix_first_s4_lp
        ret
mix_first_s5:
mix_first_s5_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np24
        inc  d
np24:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np25
        inc  d
np25:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np26
        inc  d
np26:
        dec  b
        jp   nz,mix_first_s5_lp
        ret
mix_first_s6:
mix_first_s6_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np28
        inc  d
np28:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np29
        inc  d
np29:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np30
        inc  d
np30:
        dec  b
        jp   nz,mix_first_s6_lp
        ret
mix_first_s7:
mix_first_s7_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np32
        inc  d
np32:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np33
        inc  d
np33:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np34
        inc  d
np34:
        dec  b
        jp   nz,mix_first_s7_lp
        ret
mix_first_s8:
mix_first_s8_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(G_PAD)         ; pace: hold the tick to the frame's period
pd36:
        dec  a
        jr   nz,pd36
        ld   (hl),0   ; muted, but the position still advances
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np37
        inc  d
np37:
        ld   (hl),0   ; muted, but the position still advances
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np38
        inc  d
np38:
        ld   (hl),0   ; muted, but the position still advances
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np39
        inc  d
np39:
        dec  b
        jp   nz,mix_first_s8_lp
        ret
mix_first_s9:
mix_first_s9_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(G_PAD)         ; pace: hold the tick to the frame's period
pd40:
        dec  a
        jr   nz,pd40
        ld   (hl),0   ; no voice: the plane's silence
        inc  l
        ld   (hl),0   ; no voice: the plane's silence
        inc  l
        ld   (hl),0   ; no voice: the plane's silence
        inc  l
        dec  b
        jp   nz,mix_first_s9_lp
        ret
mix_first1_s0:
mix_first1_s0_lp:
        ld   a,(de)             ; sample
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np41
        inc  d
np41:
        djnz mix_first1_s0_lp
        ret
mix_first1_s1:
mix_first1_s1_lp:
        ld   a,(de)             ; sample
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np42
        inc  d
np42:
        djnz mix_first1_s1_lp
        ret
mix_first1_s2:
mix_first1_s2_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np43
        inc  d
np43:
        djnz mix_first1_s2_lp
        ret
mix_first1_s3:
mix_first1_s3_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np44
        inc  d
np44:
        djnz mix_first1_s3_lp
        ret
mix_first1_s4:
mix_first1_s4_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np45
        inc  d
np45:
        djnz mix_first1_s4_lp
        ret
mix_first1_s5:
mix_first1_s5_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np46
        inc  d
np46:
        djnz mix_first1_s5_lp
        ret
mix_first1_s6:
mix_first1_s6_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np47
        inc  d
np47:
        djnz mix_first1_s6_lp
        ret
mix_first1_s7:
mix_first1_s7_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np48
        inc  d
np48:
        djnz mix_first1_s7_lp
        ret
mix_first1_s8:
mix_first1_s8_lp:
        ld   (hl),0   ; muted, but the position still advances
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np49
        inc  d
np49:
        djnz mix_first1_s8_lp
        ret
mix_first1_s9:
mix_first1_s9_lp:
        ld   (hl),0   ; no voice: the plane's silence
        inc  l
        djnz mix_first1_s9_lp
        ret
mix_first_tab:
        dw   mix_first_s0, mix_first_s1, mix_first_s2, mix_first_s3, mix_first_s4, mix_first_s5, mix_first_s6, mix_first_s7, mix_first_s8, mix_first_s9
mix_first1_tab:
        dw   mix_first1_s0, mix_first1_s1, mix_first1_s2, mix_first1_s3, mix_first1_s4, mix_first1_s5, mix_first1_s6, mix_first1_s7, mix_first1_s8, mix_first1_s9
R_FIRST_END:
R_ADD_BEG:
mix_add_s0:
mix_add_s0_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(G_PAD)         ; pace: hold the tick to the frame's period
pd53:
        dec  a
        jr   nz,pd53
        ld   a,(de)             ; sample
        add  a,(hl)
        jp   pe,ov54
dn55:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np56
        inc  d
np56:
        ld   a,(de)             ; sample
        add  a,(hl)
        jp   pe,ov57
dn58:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np59
        inc  d
np59:
        ld   a,(de)             ; sample
        add  a,(hl)
        jp   pe,ov60
dn61:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np62
        inc  d
np62:
        dec  b
        jp   nz,mix_add_s0_lp
        ret
ov54:
        jp   m,ov54_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn55
ov54_hi:
        ld   a,$7f
        jp   dn55
ov57:
        jp   m,ov57_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn58
ov57_hi:
        ld   a,$7f
        jp   dn58
ov60:
        jp   m,ov60_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn61
ov60_hi:
        ld   a,$7f
        jp   dn61
mix_add_s1:
mix_add_s1_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(de)             ; sample
        sra  a
        add  a,(hl)
        jp   pe,ov66
dn67:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np68
        inc  d
np68:
        ld   a,(de)             ; sample
        sra  a
        add  a,(hl)
        jp   pe,ov69
dn70:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np71
        inc  d
np71:
        ld   a,(de)             ; sample
        sra  a
        add  a,(hl)
        jp   pe,ov72
dn73:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np74
        inc  d
np74:
        dec  b
        jp   nz,mix_add_s1_lp
        ret
ov66:
        jp   m,ov66_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn67
ov66_hi:
        ld   a,$7f
        jp   dn67
ov69:
        jp   m,ov69_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn70
ov69_hi:
        ld   a,$7f
        jp   dn70
ov72:
        jp   m,ov72_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn73
ov72_hi:
        ld   a,$7f
        jp   dn73
mix_add_s2:
mix_add_s2_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(de)             ; sample
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov78
dn79:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np80
        inc  d
np80:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov81
dn82:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np83
        inc  d
np83:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov84
dn85:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np86
        inc  d
np86:
        dec  b
        jp   nz,mix_add_s2_lp
        ret
ov78:
        jp   m,ov78_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn79
ov78_hi:
        ld   a,$7f
        jp   dn79
ov81:
        jp   m,ov81_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn82
ov81_hi:
        ld   a,$7f
        jp   dn82
ov84:
        jp   m,ov84_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn85
ov84_hi:
        ld   a,$7f
        jp   dn85
mix_add_s3:
mix_add_s3_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov90
dn91:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np92
        inc  d
np92:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov93
dn94:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np95
        inc  d
np95:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov96
dn97:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np98
        inc  d
np98:
        dec  b
        jp   nz,mix_add_s3_lp
        ret
ov90:
        jp   m,ov90_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn91
ov90_hi:
        ld   a,$7f
        jp   dn91
ov93:
        jp   m,ov93_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn94
ov93_hi:
        ld   a,$7f
        jp   dn94
ov96:
        jp   m,ov96_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn97
ov96_hi:
        ld   a,$7f
        jp   dn97
mix_add_s4:
mix_add_s4_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov102
dn103:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np104
        inc  d
np104:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov105
dn106:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np107
        inc  d
np107:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov108
dn109:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np110
        inc  d
np110:
        dec  b
        jp   nz,mix_add_s4_lp
        ret
ov102:
        jp   m,ov102_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn103
ov102_hi:
        ld   a,$7f
        jp   dn103
ov105:
        jp   m,ov105_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn106
ov105_hi:
        ld   a,$7f
        jp   dn106
ov108:
        jp   m,ov108_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn109
ov108_hi:
        ld   a,$7f
        jp   dn109
mix_add_s5:
mix_add_s5_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov114
dn115:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np116
        inc  d
np116:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov117
dn118:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np119
        inc  d
np119:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov120
dn121:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np122
        inc  d
np122:
        dec  b
        jp   nz,mix_add_s5_lp
        ret
ov114:
        jp   m,ov114_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn115
ov114_hi:
        ld   a,$7f
        jp   dn115
ov117:
        jp   m,ov117_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn118
ov117_hi:
        ld   a,$7f
        jp   dn118
ov120:
        jp   m,ov120_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn121
ov120_hi:
        ld   a,$7f
        jp   dn121
mix_add_s6:
mix_add_s6_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov126
dn127:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np128
        inc  d
np128:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov129
dn130:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np131
        inc  d
np131:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov132
dn133:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np134
        inc  d
np134:
        dec  b
        jp   nz,mix_add_s6_lp
        ret
ov126:
        jp   m,ov126_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn127
ov126_hi:
        ld   a,$7f
        jp   dn127
ov129:
        jp   m,ov129_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn130
ov129_hi:
        ld   a,$7f
        jp   dn130
ov132:
        jp   m,ov132_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn133
ov132_hi:
        ld   a,$7f
        jp   dn133
mix_add_s7:
mix_add_s7_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov138
dn139:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np140
        inc  d
np140:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov141
dn142:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np143
        inc  d
np143:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov144
dn145:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np146
        inc  d
np146:
        dec  b
        jp   nz,mix_add_s7_lp
        ret
ov138:
        jp   m,ov138_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn139
ov138_hi:
        ld   a,$7f
        jp   dn139
ov141:
        jp   m,ov141_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn142
ov141_hi:
        ld   a,$7f
        jp   dn142
ov144:
        jp   m,ov144_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn145
ov144_hi:
        ld   a,$7f
        jp   dn145
mix_add_s8:
mix_add_s8_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(G_PAD)         ; pace: hold the tick to the frame's period
pd148:
        dec  a
        jr   nz,pd148
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np149
        inc  d
np149:
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np150
        inc  d
np150:
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np151
        inc  d
np151:
        dec  b
        jp   nz,mix_add_s8_lp
        ret
mix_add_s9:
mix_add_s9_lp:
        ld   a,(iy+0)          ; the sample the LAST frame finished
        xor  $80               ; the plane is signed, the DAC is not
        ld   (YM_DATA0),a      ; $2A: latched once a frame, then fed blind
        inc  iy
        ld   a,(G_PAD)         ; pace: hold the tick to the frame's period
pd152:
        dec  a
        jr   nz,pd152
        inc  l
        inc  l
        inc  l
        dec  b
        jp   nz,mix_add_s9_lp
        ret
mix_add1_s0:
mix_add1_s0_lp:
        ld   a,(de)             ; sample
        add  a,(hl)
        jp   pe,ov153
dn154:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np155
        inc  d
np155:
        djnz mix_add1_s0_lp
        ret
ov153:
        jp   m,ov153_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn154
ov153_hi:
        ld   a,$7f
        jp   dn154
mix_add1_s1:
mix_add1_s1_lp:
        ld   a,(de)             ; sample
        sra  a
        add  a,(hl)
        jp   pe,ov156
dn157:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np158
        inc  d
np158:
        djnz mix_add1_s1_lp
        ret
ov156:
        jp   m,ov156_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn157
ov156_hi:
        ld   a,$7f
        jp   dn157
mix_add1_s2:
mix_add1_s2_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov159
dn160:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np161
        inc  d
np161:
        djnz mix_add1_s2_lp
        ret
ov159:
        jp   m,ov159_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn160
ov159_hi:
        ld   a,$7f
        jp   dn160
mix_add1_s3:
mix_add1_s3_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov162
dn163:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np164
        inc  d
np164:
        djnz mix_add1_s3_lp
        ret
ov162:
        jp   m,ov162_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn163
ov162_hi:
        ld   a,$7f
        jp   dn163
mix_add1_s4:
mix_add1_s4_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov165
dn166:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np167
        inc  d
np167:
        djnz mix_add1_s4_lp
        ret
ov165:
        jp   m,ov165_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn166
ov165_hi:
        ld   a,$7f
        jp   dn166
mix_add1_s5:
mix_add1_s5_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov168
dn169:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np170
        inc  d
np170:
        djnz mix_add1_s5_lp
        ret
ov168:
        jp   m,ov168_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn169
ov168_hi:
        ld   a,$7f
        jp   dn169
mix_add1_s6:
mix_add1_s6_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov171
dn172:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np173
        inc  d
np173:
        djnz mix_add1_s6_lp
        ret
ov171:
        jp   m,ov171_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn172
ov171_hi:
        ld   a,$7f
        jp   dn172
mix_add1_s7:
mix_add1_s7_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov174
dn175:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np176
        inc  d
np176:
        djnz mix_add1_s7_lp
        ret
ov174:
        jp   m,ov174_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn175
ov174_hi:
        ld   a,$7f
        jp   dn175
mix_add1_s8:
mix_add1_s8_lp:
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np177
        inc  d
np177:
        djnz mix_add1_s8_lp
        ret
mix_add1_s9:
mix_add1_s9_lp:
        inc  l
        djnz mix_add1_s9_lp
        ret
mix_add_tab:
        dw   mix_add_s0, mix_add_s1, mix_add_s2, mix_add_s3, mix_add_s4, mix_add_s5, mix_add_s6, mix_add_s7, mix_add_s8, mix_add_s9
mix_add1_tab:
        dw   mix_add1_s0, mix_add1_s1, mix_add1_s2, mix_add1_s3, mix_add1_s4, mix_add1_s5, mix_add1_s6, mix_add1_s7, mix_add1_s8, mix_add1_s9
R_ADD_END:
R_OUT_BEG:
; flush_rest: HL = plane cursor, B = samples still owed (0 = none).
flush_rest:
        ld   a,b
        or   a
        ret  z
fr_loop:
        ld   a,(hl)
        xor  $80
        inc  l
        ld   (YM_DATA0),a       ; $2A is fed blind — driver.md §5.1
        djnz fr_loop
        ret
R_OUT_END:

; mix_seg: run one segment of a voice pass — the ticks the caller has already
; proved cannot cross a loop or end boundary.
;
;   IX = voice struct      A  = 0 for the storing voice, nonzero to accumulate
;   B  = ticks in this segment (1..255)
;   L  = buffer tick index (H is set here)
;
; Splits B into unrolled iterations plus a single-tick remainder, loads the
; register file, and returns the advanced position in the struct.
mix_seg:
        ld   (G_SEG_L),a        ; accumulate flag (A is needed for other things)
        ld   a,l
        ld   (G_SEG_I),a        ; buffer index to resume at
        ld   hl,G_NSEG          ; what this frame is costing the NEXT one
        inc  (hl)

        ; --- split B into unrolled iterations + single-tick remainder ---
        ; Done here, before the register file is loaded, because it is the last
        ; moment HL and DE are free.
        ld   h,0                ; the cadence is 3, so the divide is a table
        ld   l,b
        ld   de,div_tab
        add  hl,de
        ld   a,(hl)
        ld   (G_SEG_N),a        ; iterations
        ld   l,a
        add  a,a
        add  a,l                ; 3 x iterations
        ld   l,a
        ld   a,b
        sub  l
        ld   (G_SEG_R),a        ; remainder ticks (0..2)
        ; --- load the register file ---
        ld   l,(ix+PV_FRAC)
        ld   h,(ix+PV_FRAC+1)
        ld   e,(ix+PV_INCF)
        ld   d,(ix+PV_INCF+1)
        exx                     ; HL' = frac, DE' = incFrac
        ld   c,(ix+PV_INCI)     ; AFTER the exx — exx swaps BC too
        ld   e,(ix+PV_PTR)
        ld   d,(ix+PV_PTR+1)
        ld   a,(G_SEG_I)
        ld   l,a
        ld   a,(G_MIXP)         ; the plane being mixed this frame
        ld   h,a
        ; --- unrolled iterations ---
        ld   a,(G_SEG_N)
        ld   b,a
        or   a
        jr   z,ms_rem
        push bc
        call ms_call_unrolled
        pop  bc
ms_rem:
        ld   a,(G_SEG_R)
        or   a
        jr   z,ms_done
        ld   b,a
        call ms_call_single
ms_done:
        ; --- write the position back ---
        ld   (ix+PV_PTR),e
        ld   (ix+PV_PTR+1),d
        ld   a,l
        ld   (G_SEG_I),a        ; resume index for the caller
        exx
        ld   (ix+PV_FRAC),l
        ld   (ix+PV_FRAC+1),h
        exx
        ld   a,(G_SEG_I)
        ld   l,a
        ret

; Entry to the shift-specialised copies. Both are bare self-modified jumps that
; ms_bind has already pointed at the right loop, so the per-segment path is ONE
; instruction — the register file in HL/DE is live here and nothing may touch
; it. The copy rets to mix_seg, which is why these are jumps and not calls.
ms_call_unrolled:
ms_go_u:
        jp   0
ms_call_single:
ms_go_s:
        jp   0

; ms_bind: point both entries at this PASS's copies, and size its pad.
;
; The copy is chosen by (role, shift) and BOTH are constant for a whole voice
; pass — a pass is one voice, and PV_SHIFT only moves when a PCM_VOL arrives,
; which happens between passes. Doing this per SEGMENT re-derived it five or six
; times a frame, along with a pad that had not changed either: ~1,700 cycles spent on values that were
; already known. Rebind after anything that changes the voice under the pass —
; engine.z80 does it at pass entry and again when a pass falls through to the
; silent loop.
ms_bind:
        push hl
        push de
        push bc
        ld   a,(ix+PV_SHIFT)
        add  a,a                ; word table
        ld   e,a
        ld   d,0
        ld   hl,mix_add_tab
        ld   a,(G_SEG_L)
        or   a
        jr   nz,msb_u
        ld   hl,mix_first_tab
msb_u:
        add  hl,de
        ld   a,(hl)
        inc  hl
        ld   h,(hl)
        ld   l,a
        ld   (ms_go_u+1),hl
        ld   hl,mix_add1_tab
        ld   a,(G_SEG_L)
        or   a
        jr   nz,msb_s
        ld   hl,mix_first1_tab
msb_s:
        add  hl,de
        ld   a,(hl)
        inc  hl
        ld   h,(hl)
        ld   l,a
        ld   (ms_go_s+1),hl
        call ms_set_pad
        pop  bc
        pop  de
        pop  hl
        ret


; ms_set_pad: G_PAD = base(role, shift) − G_DEBT, the pad this segment's copies
; hold to. Floored at 1 — the pad loop counts DOWN from G_PAD, so zero would be
; 256 — which is also what a frame with no slack left to give does.
ms_set_pad:
        call pcm_debt           ; re-estimated per segment: a frame that turns
                                ; out heavier than its predecessor tightens from
                                ; the segment where it finds out
        ld   hl,pad_add_tab
        ld   a,(G_SEG_L)
        or   a
        jr   nz,msp_role
        ld   hl,pad_first_tab
msp_role:
        ld   d,0
        ld   e,(ix+PV_SHIFT)
        add  hl,de
        ld   a,(hl)
        or   a
        ret  z                  ; this copy carries no pad — nothing to hold to
        ld   hl,G_DEBT
        sub  (hl)
        jr   c,msp_min
        or   a
        jr   nz,msp_store
msp_min:
        ld   a,1
msp_store:
        ld   (G_PAD),a
        ret

; Pad each copy would need to hit 340 cycles with the frame to itself, by
; shift, mute last. 0 = the copy is already at or over the period, and is
; generated without pad code at all.
pad_first_tab:
        db   5,3,2,0,0,0,0,0,6,14
pad_add_tab:
        db   2,0,0,0,0,0,0,0,8,16

; debt_tab[segments] = 7200 + segments x 2400 cycles of per-frame overhead, in
; 16-cycle pad units, spread over all PCM_MIX_R samples of the frame.
debt_tab:
        db   3,3,4,5,6,7,8,9,9,10,11,12,13,14,15,15
        db   16,17,18,19,20,21,21,22,23,24,25,26,27,27,28,29

; div_tab[n] = n / 3, for n = 0..PCM_MIX_R
div_tab:
        db   0,0,0,1,1,1,2,2,2,3,3,3,4,4,4,5
        db   5,5,6,6,6,7,7,7,8,8,8,9,9,9,10,10
        db   10,11,11,11,12,12,12,13,13,13,14,14,14,15,15,15
        db   16,16,16,17,17,17,18,18,18,19,19,19,20,20,20,21
        db   21,21,22,22,22,23,23,23,24,24,24,25,25,25,26,26
        db   26,27,27,27,28,28,28,29,29,29,30,30,30,31,31,31
        db   32,32,32,33,33,33,34,34,34,35,35,35,36,36,36,37
        db   37,37,38,38,38,39,39,39,40,40,40,41,41,41,42,42
        db   42,43,43,43,44,44,44,45,45,45,46,46,46,47,47,47
        db   48,48,48,49,49,49,50,50,50,51,51,51,52,52,52,53
        db   53,53,54,54,54,55,55,55,56,56,56,57,57,57,58,58