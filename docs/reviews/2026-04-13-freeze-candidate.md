# GMLisp v0.1 Freeze Candidate Review (2026-04-13)

## 1. Candidate Info

1. Candidate ID: freeze-candidate-2026-04-13
2. Date: 2026-04-13
3. Reviewer(s): okamura
4. Compiler version: gmlisp-tools-0.1.0-draft
5. Target profile: strict-v0.1

## 2. Scope Confirmation

1. Included documents:
   - docs/spec-v0.1-draft.md
   - docs/command-table-v0.1-draft.md
   - docs/ir-v0.1-draft.md
   - docs/gmb-format-v0.1-draft.md
   - docs/freeze-checklist-v0.1.md
   - docs/compiler-contract-v0.1-draft.md
   - docs/gmldrv-decoder-contract-v0.1-draft.md
2. Excluded items (deferred to v0.2 — see §8):
   - REG_WRITE and FM3_MODE IR commands
   - CSM_ON / CSM_OFF / CSM_RATE commands
   - PCM/DAC streaming (no GML-level syntax or IR command)
   - Full semantic diagnostics catalog
   - Opcode table final freeze (still draft pending driver integration)
   - GMLDRV implementation
3. Intended freeze level: candidate

## 3. Artifact Paths

1. Demo source files:
   - examples/source/demo1-stage-loop.gml
   - examples/source/demo2-event-recovery.gml
2. IR snapshot files:
   - examples/ir/demo1-stage-loop.ir.canonical.json
   - examples/ir/demo2-event-recovery.ir.canonical.json
3. GMB files:
   - examples/gmb/demo1-stage-loop.gmb
   - examples/gmb/demo2-event-recovery.gmb
4. Validation logs: generated by `npm run check:ir-demos` and `npm run check:gmb-demos`

## 4. Demo Validation

1. demo1-stage-loop.gml result: pass (strict mode, no diagnostics errors)
   - Exercises: loop/marker, base/delta FM param modulation, def constants, tuplet sugar
2. demo2-event-recovery.gml result: pass (strict mode, no diagnostics errors)
   - Exercises: base/delta TEMPO_SCALE modulation, loop with param-add, marker/jump recovery
3. Loop/marker stability notes: LOOP_BEGIN/LOOP_END pairs fully validated; JUMP targets resolve
4. base/delta recovery notes: PARAM_SET(base) + PARAM_ADD(delta) round-trip verified in IR

## 5. IR Validation

1. Deterministic output check: pass — `npm run check:ir-demos` IR match OK for both demos
2. Marker/jump resolution check: pass — all jump targets resolve to defined markers
3. Diagnostic quality check: pass for current demo scenarios; full catalog not yet required for freeze

## 6. GMB Validation

1. Header validity: pass — version=0.1, sections=3 for both demos
2. Section directory validity: pass — offsets and sizes consistent
3. Track/event offset validity: pass — demo1: 19 events / 324 bytes; demo2: 20 events / 336 bytes
4. Decoder compatibility fixtures: pass — `npm run check:gmb-fixtures`

## 7. Freeze Gate Status

1. Two demos completed: yes
2. Loop and marker stable: yes
3. base/delta recovery verified: yes
4. IR command set judged minimal: yes
5. GMB layout accepted for driver use: yes (pending final opcode freeze at driver integration)

## 8. Risks and Decisions

### Accepted risks

1. Opcode table is provisional — numeric assignments are draft; final values will be confirmed
   against GMLDRV when driver integration begins. Binary format is structurally frozen.
2. Semantic diagnostics coverage is partial — unknown targets produce errors; other edge cases
   (e.g. phrase nesting, duplicate track names) are not yet exhaustively checked. Sufficient for
   current demo scope.

### Deferred to v0.2

1. REG_WRITE — raw YM2612 register write; parser reserved, compiler rejects in strict mode
2. FM3_MODE — FM3 independent-operator mode; same reservation policy as REG_WRITE
3. CSM_ON / CSM_OFF / CSM_RATE — complex speech-synthesis mode; out of scope for v0.1 music demo
4. PCM/DAC streaming — YM2612 DAC (ch6) hardware support exists in ym2612.js, but no IR command
   or GML syntax is defined; deferred until sample playback design is settled
5. Full semantic diagnostics catalog — additional error codes and coverage beyond current checks
6. Multi-track channel allocation — voice stealing and track splitting not yet implemented

### No blocking issues

All freeze gate criteria are satisfied. The only outstanding item (final opcode freeze) is
driver-integration-time work and does not block the authoring-to-binary pipeline.

## 9. Changes Since Previous Checkpoint (2026-04-09)

1. feat: notes sugar syntax (33f57b2)
2. feat: multi-KV param-set / param-add (ac4db8f)
3. refactor: part → track rename (4aaa405)
4. feat: def / defn compile-time constants and template macros (ab06b21)
5. feat: GML-specific TextMate grammar for VS Code (f8c9699)
6. feat: tuplet sugar syntax (8caa367)
7. fix: formatter splitting :tempo-scale incorrectly (a1004a2)
8. fix: ir-player scheduler tuning and AudioContext resume (b1b543a)
9. docs: default values, def/defn spec subsection, gml-syntax in README/roadmap

## 10. Decision

1. Freeze accepted: **yes, as freeze candidate**
2. Conditions: final opcode byte assignments to be confirmed at GMLDRV integration start
3. Next step: tag v0.1-candidate; begin Phase 3 (GMLDRV) planning

## 11. Action Items

1. Owner: okamura — tag v0.1-candidate in git ✓ (52f4e20)
2. Owner: okamura — begin GMLDRV decoder implementation against frozen GMB format
3. Owner: okamura — finalize opcode table as first GMLDRV integration milestone

## 12. Post-Candidate Fixes (same session, 2026-04-13)

The following fixes were applied after the initial freeze candidate tag and are included
in the final v0.1-candidate commit:

1. **fix(player): ahead-of-time scheduling via timestamped worklet writes** (e5557d6)
   - Root cause: `setTimeout` per event in `_scheduleLoop` introduced ±10–20ms timer jitter,
     making rhythm audibly irregular.
   - Fix: all register writes carry an AudioContext timestamp (`when`); the AudioWorklet
     inserts them into a frame-sorted `_timedQueue` and drains at each block boundary.
     Worst-case timing error is now ±128/48000 ≈ 2.7ms (one worklet block).
   - Audition result: timing confirmed correct by author.

2. **feat: redesign demos as bar-aligned musical loops** (e5557d6, 56433bc)
   - demo1-stage-loop: rewritten as 6-bar C-major loop (A×2 + B section), 38 IR events, 516B GMB.
     Previous version had bar-misaligned loop body (360t) and turn section (180t).
   - demo2-event-recovery: rewritten as 8-bar A-minor loop (intro 2bar + stress×4 + recovery 2bar),
     47 IR events, 604B GMB. Previous version had misaligned phrase lengths.
   - Canonical IR and GMB regenerated; IR match OK × 2, GMB valid × 2.

3. **fix(player): auto-stop on preset change or file load** (52f4e20)
   - Selecting a different preset or browsing a local file now calls `onStop()` automatically
     to prevent stale playback state.

All checks pass at final HEAD. Tag v0.1-candidate updated to 52f4e20.
