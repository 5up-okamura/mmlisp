# examples/ir

Canonical IR snapshots for freeze validation.

Naming convention:

1. <demo-id>.ir.json
2. <demo-id>.ir.canonical.json

Rules:

1. Keep files deterministic and sorted.
2. Regenerate when language or normalization rules change.
3. Link each snapshot to notes under examples/notes.
