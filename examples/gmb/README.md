# examples/gmb

Exported GMB binaries for freeze validation.

Naming convention:

1. <demo-id>.gmb
2. <demo-id>.meta.txt

Rules:

1. Keep one exported binary per demo checkpoint.
2. Record header and section validation results in examples/notes.
3. Do not overwrite historical checkpoints; add suffixes when needed.
