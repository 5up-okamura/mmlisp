; ===========================================================================
; PCM soft mixer core — voice-outer, i8sat, R = 175, unroll 2
;
; GENERATED by tools/gen-mixer.mjs — do not edit. Regenerate instead.
; Design and measured cost: docs/driver.md §5.3 / §5.3.1.
;
; Included by src/engine.z80 (and by the bench image). The includer owns the RAM
; map and the PV_* struct offsets; this file defines only PCM_MIX_R and the code.
;
; Each voice owns the register file for its whole pass and writes into a
; frame-long mix buffer; a final pass feeds the DAC. The inner loops carry NO
; loop/end bounds check — the caller splits the pass into segments that cannot
; cross a boundary, which is exactly what a voice-outer pass buys.
; ===========================================================================

PCM_MIX_R   equ 175
MIX_UNROLL  equ 2

R_FIRST_BEG:
mix_first_s0:
mix_first_s0_lp:
        ld   a,(de)             ; sample
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np0
        inc  d
np0:
        ld   a,(de)             ; sample
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np1
        inc  d
np1:
        dec  b
        jp   nz,mix_first_s0_lp
        ret
mix_first_s1:
mix_first_s1_lp:
        ld   a,(de)             ; sample
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np2
        inc  d
np2:
        ld   a,(de)             ; sample
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np3
        inc  d
np3:
        dec  b
        jp   nz,mix_first_s1_lp
        ret
mix_first_s2:
mix_first_s2_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np4
        inc  d
np4:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np5
        inc  d
np5:
        dec  b
        jp   nz,mix_first_s2_lp
        ret
mix_first_s3:
mix_first_s3_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np6
        inc  d
np6:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np7
        inc  d
np7:
        dec  b
        jp   nz,mix_first_s3_lp
        ret
mix_first_s4:
mix_first_s4_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np8
        inc  d
np8:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np9
        inc  d
np9:
        dec  b
        jp   nz,mix_first_s4_lp
        ret
mix_first_s5:
mix_first_s5_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np10
        inc  d
np10:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np11
        inc  d
np11:
        dec  b
        jp   nz,mix_first_s5_lp
        ret
mix_first_s6:
mix_first_s6_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np12
        inc  d
np12:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np13
        inc  d
np13:
        dec  b
        jp   nz,mix_first_s6_lp
        ret
mix_first_s7:
mix_first_s7_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np14
        inc  d
np14:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np15
        inc  d
np15:
        dec  b
        jp   nz,mix_first_s7_lp
        ret
mix_first_s8:
mix_first_s8_lp:
        ld   (hl),0   ; muted, but the position still advances
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np16
        inc  d
np16:
        ld   (hl),0   ; muted, but the position still advances
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np17
        inc  d
np17:
        dec  b
        jp   nz,mix_first_s8_lp
        ret
mix_first1_s0:
mix_first1_s0_lp:
        ld   a,(de)             ; sample
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np18
        inc  d
np18:
        djnz mix_first1_s0_lp
        ret
mix_first1_s1:
mix_first1_s1_lp:
        ld   a,(de)             ; sample
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np19
        inc  d
np19:
        djnz mix_first1_s1_lp
        ret
mix_first1_s2:
mix_first1_s2_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np20
        inc  d
np20:
        djnz mix_first1_s2_lp
        ret
mix_first1_s3:
mix_first1_s3_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np21
        inc  d
np21:
        djnz mix_first1_s3_lp
        ret
mix_first1_s4:
mix_first1_s4_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np22
        inc  d
np22:
        djnz mix_first1_s4_lp
        ret
mix_first1_s5:
mix_first1_s5_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np23
        inc  d
np23:
        djnz mix_first1_s5_lp
        ret
mix_first1_s6:
mix_first1_s6_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np24
        inc  d
np24:
        djnz mix_first1_s6_lp
        ret
mix_first1_s7:
mix_first1_s7_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np25
        inc  d
np25:
        djnz mix_first1_s7_lp
        ret
mix_first1_s8:
mix_first1_s8_lp:
        ld   (hl),0   ; muted, but the position still advances
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np26
        inc  d
np26:
        djnz mix_first1_s8_lp
        ret
mix_first_tab:
        dw   mix_first_s0, mix_first_s1, mix_first_s2, mix_first_s3, mix_first_s4, mix_first_s5, mix_first_s6, mix_first_s7, mix_first_s8
mix_first1_tab:
        dw   mix_first1_s0, mix_first1_s1, mix_first1_s2, mix_first1_s3, mix_first1_s4, mix_first1_s5, mix_first1_s6, mix_first1_s7, mix_first1_s8
R_FIRST_END:
R_ADD_BEG:
mix_add_s0:
mix_add_s0_lp:
        ld   a,(de)             ; sample
        add  a,(hl)
        jp   pe,ov27
dn28:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np29
        inc  d
np29:
        ld   a,(de)             ; sample
        add  a,(hl)
        jp   pe,ov30
dn31:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np32
        inc  d
np32:
        dec  b
        jp   nz,mix_add_s0_lp
        ret
ov27:
        jp   m,ov27_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn28
ov27_hi:
        ld   a,$7f
        jp   dn28
ov30:
        jp   m,ov30_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn31
ov30_hi:
        ld   a,$7f
        jp   dn31
mix_add_s1:
mix_add_s1_lp:
        ld   a,(de)             ; sample
        sra  a
        add  a,(hl)
        jp   pe,ov33
dn34:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np35
        inc  d
np35:
        ld   a,(de)             ; sample
        sra  a
        add  a,(hl)
        jp   pe,ov36
dn37:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np38
        inc  d
np38:
        dec  b
        jp   nz,mix_add_s1_lp
        ret
ov33:
        jp   m,ov33_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn34
ov33_hi:
        ld   a,$7f
        jp   dn34
ov36:
        jp   m,ov36_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn37
ov36_hi:
        ld   a,$7f
        jp   dn37
mix_add_s2:
mix_add_s2_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov39
dn40:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np41
        inc  d
np41:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov42
dn43:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np44
        inc  d
np44:
        dec  b
        jp   nz,mix_add_s2_lp
        ret
ov39:
        jp   m,ov39_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn40
ov39_hi:
        ld   a,$7f
        jp   dn40
ov42:
        jp   m,ov42_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn43
ov42_hi:
        ld   a,$7f
        jp   dn43
mix_add_s3:
mix_add_s3_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov45
dn46:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np47
        inc  d
np47:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov48
dn49:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np50
        inc  d
np50:
        dec  b
        jp   nz,mix_add_s3_lp
        ret
ov45:
        jp   m,ov45_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn46
ov45_hi:
        ld   a,$7f
        jp   dn46
ov48:
        jp   m,ov48_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn49
ov48_hi:
        ld   a,$7f
        jp   dn49
mix_add_s4:
mix_add_s4_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov51
dn52:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np53
        inc  d
np53:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov54
dn55:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np56
        inc  d
np56:
        dec  b
        jp   nz,mix_add_s4_lp
        ret
ov51:
        jp   m,ov51_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn52
ov51_hi:
        ld   a,$7f
        jp   dn52
ov54:
        jp   m,ov54_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn55
ov54_hi:
        ld   a,$7f
        jp   dn55
mix_add_s5:
mix_add_s5_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov57
dn58:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np59
        inc  d
np59:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov60
dn61:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np62
        inc  d
np62:
        dec  b
        jp   nz,mix_add_s5_lp
        ret
ov57:
        jp   m,ov57_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn58
ov57_hi:
        ld   a,$7f
        jp   dn58
ov60:
        jp   m,ov60_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn61
ov60_hi:
        ld   a,$7f
        jp   dn61
mix_add_s6:
mix_add_s6_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov63
dn64:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np65
        inc  d
np65:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov66
dn67:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np68
        inc  d
np68:
        dec  b
        jp   nz,mix_add_s6_lp
        ret
ov63:
        jp   m,ov63_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn64
ov63_hi:
        ld   a,$7f
        jp   dn64
ov66:
        jp   m,ov66_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn67
ov66_hi:
        ld   a,$7f
        jp   dn67
mix_add_s7:
mix_add_s7_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov69
dn70:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np71
        inc  d
np71:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov72
dn73:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np74
        inc  d
np74:
        dec  b
        jp   nz,mix_add_s7_lp
        ret
ov69:
        jp   m,ov69_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn70
ov69_hi:
        ld   a,$7f
        jp   dn70
ov72:
        jp   m,ov72_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn73
ov72_hi:
        ld   a,$7f
        jp   dn73
mix_add_s8:
mix_add_s8_lp:
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np75
        inc  d
np75:
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np76
        inc  d
np76:
        dec  b
        jp   nz,mix_add_s8_lp
        ret
mix_add1_s0:
mix_add1_s0_lp:
        ld   a,(de)             ; sample
        add  a,(hl)
        jp   pe,ov77
dn78:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np79
        inc  d
np79:
        djnz mix_add1_s0_lp
        ret
ov77:
        jp   m,ov77_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn78
ov77_hi:
        ld   a,$7f
        jp   dn78
mix_add1_s1:
mix_add1_s1_lp:
        ld   a,(de)             ; sample
        sra  a
        add  a,(hl)
        jp   pe,ov80
dn81:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np82
        inc  d
np82:
        djnz mix_add1_s1_lp
        ret
ov80:
        jp   m,ov80_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn81
ov80_hi:
        ld   a,$7f
        jp   dn81
mix_add1_s2:
mix_add1_s2_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov83
dn84:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np85
        inc  d
np85:
        djnz mix_add1_s2_lp
        ret
ov83:
        jp   m,ov83_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn84
ov83_hi:
        ld   a,$7f
        jp   dn84
mix_add1_s3:
mix_add1_s3_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov86
dn87:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np88
        inc  d
np88:
        djnz mix_add1_s3_lp
        ret
ov86:
        jp   m,ov86_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn87
ov86_hi:
        ld   a,$7f
        jp   dn87
mix_add1_s4:
mix_add1_s4_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov89
dn90:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np91
        inc  d
np91:
        djnz mix_add1_s4_lp
        ret
ov89:
        jp   m,ov89_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn90
ov89_hi:
        ld   a,$7f
        jp   dn90
mix_add1_s5:
mix_add1_s5_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov92
dn93:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np94
        inc  d
np94:
        djnz mix_add1_s5_lp
        ret
ov92:
        jp   m,ov92_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn93
ov92_hi:
        ld   a,$7f
        jp   dn93
mix_add1_s6:
mix_add1_s6_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov95
dn96:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np97
        inc  d
np97:
        djnz mix_add1_s6_lp
        ret
ov95:
        jp   m,ov95_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn96
ov95_hi:
        ld   a,$7f
        jp   dn96
mix_add1_s7:
mix_add1_s7_lp:
        ld   a,(de)             ; sample
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        sra  a
        add  a,(hl)
        jp   pe,ov98
dn99:
        ld   (hl),a
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np100
        inc  d
np100:
        djnz mix_add1_s7_lp
        ret
ov98:
        jp   m,ov98_hi        ; overflow inverts the sign, so a negative
        ld   a,$80             ; result means the true value was positive
        jp   dn99
ov98_hi:
        ld   a,$7f
        jp   dn99
mix_add1_s8:
mix_add1_s8_lp:
        inc  l
        exx
        add  hl,de             ; frac += incFrac
        exx                    ; flag-transparent: the carry survives
        ld   a,e
        adc  a,c               ; ptr += incInt + frac carry
        ld   e,a
        jr   nc,np101
        inc  d
np101:
        djnz mix_add1_s8_lp
        ret
mix_add_tab:
        dw   mix_add_s0, mix_add_s1, mix_add_s2, mix_add_s3, mix_add_s4, mix_add_s5, mix_add_s6, mix_add_s7, mix_add_s8
mix_add1_tab:
        dw   mix_add1_s0, mix_add1_s1, mix_add1_s2, mix_add1_s3, mix_add1_s4, mix_add1_s5, mix_add1_s6, mix_add1_s7, mix_add1_s8
R_ADD_END:
R_OUT_BEG:
; The plane is signed (saturated at every add), so the output pass biases.
out_pass:
        ld   a,$2a
        ld   (YM_ADDR0),a
        ld   hl,MIXLO
        ld   b,PCM_MIX_R
os_loop:
        ld   a,(hl)
        xor  $80
        inc  l
        ld   (YM_DATA0),a       ; $2A is fed blind — driver.md §5.1
        djnz os_loop
        ret
R_OUT_END:

; fill_silence: HL = plane cursor, B = ticks remaining. Nothing to do when B = 0.
fill_silence:
        ld   a,b
        or   a
        ret  z
fs_lp:
        ld   (hl),0
        inc  l
        djnz fs_lp
        ret

; mix_seg: run one segment of a voice pass — the ticks the caller has already
; proved cannot cross a loop or end boundary.
;
;   IX = voice struct      A  = 0 for the storing voice, nonzero to accumulate
;   B  = ticks in this segment (1..255)
;   L  = buffer tick index (H is set here)
;
; Splits B into unrolled iterations plus a single-tick remainder, loads the
; register file, and returns the advanced position in the struct.
mix_seg:
        ld   (G_SEG_L),a        ; accumulate flag (A is needed for other things)
        ld   a,b
        ld   (G_SEG_N),a        ; ticks in this segment
        ld   a,l
        ld   (G_SEG_I),a        ; buffer index to resume at
        ; --- load the register file ---
        ld   l,(ix+PV_FRAC)
        ld   h,(ix+PV_FRAC+1)
        ld   e,(ix+PV_INCF)
        ld   d,(ix+PV_INCF+1)
        exx                     ; HL' = frac, DE' = incFrac
        ld   c,(ix+PV_INCI)     ; AFTER the exx — exx swaps BC too
        ld   e,(ix+PV_PTR)
        ld   d,(ix+PV_PTR+1)
        ld   a,(G_SEG_I)
        ld   l,a
        ld   h,MIXLO>>8
        ; --- unrolled iterations: B / MIX_UNROLL ---
        ld   a,(G_SEG_N)
        ld   b,a
        and  MIX_UNROLL-1
        ld   (G_SEG_R),a        ; remainder ticks
        ld   a,b
        ; B = ticks / MIX_UNROLL (MIX_UNROLL is a power of two)
        srl  a
        ld   b,a
        or   a
        jr   z,ms_rem
        push bc
        call ms_call_unrolled
        pop  bc
ms_rem:
        ld   a,(G_SEG_R)
        or   a
        jr   z,ms_done
        ld   b,a
        call ms_call_single
ms_done:
        ; --- write the position back ---
        ld   (ix+PV_PTR),e
        ld   (ix+PV_PTR+1),d
        ld   a,l
        ld   (G_SEG_I),a        ; resume index for the caller
        exx
        ld   (ix+PV_FRAC),l
        ld   (ix+PV_FRAC+1),h
        exx
        ld   a,(G_SEG_I)
        ld   l,a
        ret

; Pick the shift-specialised copy out of the right table. Done per segment (a
; handful of times per frame), never per tick.
ms_call_unrolled:
        push hl
        ld   hl,mix_add_tab
        ld   a,(G_SEG_L)
        or   a
        jr   nz,mcu_have
        ld   hl,mix_first_tab
mcu_have:
        jr   ms_enter
ms_call_single:
        push hl
        ld   hl,mix_add1_tab
        ld   a,(G_SEG_L)
        or   a
        jr   nz,mcs_have
        ld   hl,mix_first1_tab
mcs_have:
ms_enter:
        ld   a,(ix+PV_SHIFT)
        add  a,a                ; word table
        push de
        ld   e,a
        ld   d,0
        add  hl,de
        ld   e,(hl)
        inc  hl
        ld   d,(hl)
        ld   (ms_go+1),de
        pop  de
        pop  hl
ms_go:
        jp   0                  ; tail-call the specialised loop (it rets to us)
